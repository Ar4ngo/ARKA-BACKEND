# Datasource (local by default, override with env vars in AWS)
spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/arka_db}
spring.datasource.username=${DB_USER:postgres}
spring.datasource.password=${DB_PASS:frutino2011}

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Server
server.port=${PORT:8080}

# Scheduling / reports
reports.lowstock.threshold=${LOW_STOCK_THRESHOLD:5}
reports.lowstock.output-dir=${REPORT_OUTPUT_DIR:/tmp}
reports.lowstock.cron=${REPORTS_LOWSTOCK_CRON:0 0 2 * * MON}

# Carts
carts.abandoned.cron=${CARTS_ABANDONED_CRON:0 0 3 * * *}
carts.abandoned.days=${CARTS_ABANDONED_DAYS:7}

# AWS
aws.s3.bucket=${AWS_S3_BUCKET:}
aws.s3.region=${AWS_REGION:us-east-1}

# Mail (optional)
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USER:j80188048@gmail.com}
spring.mail.password=${MAIL_PASS:soku ghdr gise oegy}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true



#